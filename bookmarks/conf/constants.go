package conf

// ContextUser key in the context holding the authenticated user
const ContextUser = "context.user"

// ContextUnitOfWork key in the context holding a unit-of-work instance
const ContextUnitOfWork = "context.unit_of_work"
